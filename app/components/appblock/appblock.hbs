<div class="row {{class}}" id="{{id}}">
    <div class="col-xs-12">
    <div class="app-block">
    	{{#each appBlockButter}} {{>PartialButton}} {{/each}}
    	{{#each appBlockAvatar}} {{>PartialAvatar}} {{/each}}
    	{{#each appBlockContent}} <p>{{{content}}} </p>{{/each}}
    	{{#each appBlockButton}} {{>PartialButton}} {{/each}}
    </div>
    </div>
    {{!-- {{#each appblockAvatar}} {{>PartialAvatar}} {{/each}} --}}
</div>